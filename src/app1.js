console.log('Hello World! Again');

var name = 'My Name';

let age=19;   //For variable with mutable value

const CITY='Nagpur'; //for constant values, not mutable

console.log(name);

const myObject = {
    name:'Ashwin',
    aga:19,
    city:'Nagpur',
    state:'Maharashtra'
};

console.log(myObject.name);
console.log(myObject);

const label = `This is sample ${myObject.name}`;
console.log(label);